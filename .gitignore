# Raw iTunes API responses — 12 MB, and fully regenerable with
# tools/fetch-itunes.ps1. The built database in data/ IS committed.
tools/cache/

# machine-local Claude Code permissions (contains absolute paths)
.claude/settings.local.json

# local
.DS_Store
Thumbs.db
*.log
